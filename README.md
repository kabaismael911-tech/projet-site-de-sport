# projet-site-de-salle-de-sport

creation d'un site de salle de sport sécuriser codé en php html  et boostrap.
le style de ce site a ete fait entierement grace a la librairie css  boostrap et quelque image.
la gestion de redirection des pages ce site a ete fait via un controller car cet méthode et plus efficace et rapide plutot que de remttre les chemins sur chaque pages.Tout les formulaires de ce site utilise la methode POST ainsi que la variable superglobale $_POST afin d'enregistrer dans la base de donnés via des requete SQL a part pour les 
Tout les formulaires de ce site utilise la methode POST ainsi que la variable superglobale $_POST afin d'enregistrer dans la base de donnés(ce site et doter de a technologie PHPMAILER qui permet de se connecter a une adresse email via un code d'application puis envoyer un email via les méthod $_POST ect) grace a cette technologie j'ai pu securiser mon application en ajoutant plusieurs option telle que l'inscription et  la reinitialisation de mots de passe (que j'ai du desactiver par mesure de sécuriter).
l

